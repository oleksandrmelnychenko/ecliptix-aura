[package]
name = "aura-agent-policy"
version.workspace = true
edition.workspace = true
description = "Agent-side policy layer: action decisions, product surfacing, shadow/pilot gates"

[dependencies]
aura-contracts = { path = "../aura-contracts" }
aura-core = { path = "../aura-core" }
